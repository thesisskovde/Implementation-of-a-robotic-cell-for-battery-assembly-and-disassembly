To run the main code correctly, the files must be loxated in the samen folder. To execute ir, simply open the Phyton file 'robotcode' and run it, the program will automatically open in RobotStudio.

In the 'tests' folder, you will find the code used to verify the vision system and the code used to adjust the HSV range.

